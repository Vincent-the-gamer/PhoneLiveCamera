{
  "name": "phone-live-camera",
  "version": "1.0.1",
  "scripts": {
    "serve": "vite",
    "build": "vite build && cp _redirects dist"
  },
  "devDependencies": {
    "vite": "^2.3.0"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
